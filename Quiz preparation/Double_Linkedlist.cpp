#include <iostream>
using namespace std;
int main ()
{
    struct node
    {
        int data;
        node* prev;
        node* next;
    };

    void insert(int val){

  
    node* n1 = new node;

    struct *node = newnode ;
    n1-> data = val;
    n1->prev = NULL;
    n1->next = NULL;
    cout << data;

  }

  insert(10);
    
}